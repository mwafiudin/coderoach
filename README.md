# Coderoach Studio

Senior product engineering studio in Jakarta. This repo is the marketing site + admin CMS.

## Stack

- **Frontend**: Next.js 15 (App Router), React 19, TypeScript
- **Styling**: Tailwind CSS v4 (with Coderoach design tokens)
- **CMS**: Payload v3 (mounted at `/admin`)
- **Database**: Postgres (Neon, connected via Vercel Marketplace)
- **Media storage**: Vercel Blob
- **Hosting**: Vercel

## Phase status

- [x] **Phase 1** — Scaffold: Next.js + Payload + Tailwind v4 + collections/globals defined
- [ ] **Phase 2** — Content: log into `/admin`, fill in placeholder content
- [ ] **Phase 3** — Frontend rebuild: migrate `_legacy/components.jsx` sections to Tailwind components, wire to Payload data

## First-time setup

### 1. Install dependencies

```bash
pnpm install
# or: npm install / yarn install
```

### 2. Setup database (Neon)

Two options:

**Option A — Vercel Marketplace (recommended, easiest)**:
1. Push this repo to GitHub.
2. Import to Vercel.
3. In Vercel project → Storage → Create Database → choose **Neon Postgres**.
4. Vercel auto-injects `DATABASE_URI` env var. Done.

**Option B — Direct Neon signup**:
1. Sign up at https://neon.tech (free tier).
2. Create a new project. Copy the connection string.
3. Set `DATABASE_URI` in `.env.local`.

### 3. Setup Vercel Blob (for media uploads)

1. In Vercel project → Storage → Create Database → choose **Blob**.
2. `BLOB_READ_WRITE_TOKEN` env var auto-injected.

For local dev, copy the token from Vercel dashboard → `.env.local`.

### 4. Configure environment

```bash
cp .env.example .env.local
```

Fill in:
- `PAYLOAD_SECRET` — generate via `openssl rand -base64 32`
- `DATABASE_URI` — from Neon
- `BLOB_READ_WRITE_TOKEN` — from Vercel Blob (optional in dev)
- `NEXT_PUBLIC_SERVER_URL` — `http://localhost:3000` for dev

### 5. Initialize database

```bash
pnpm payload migrate
pnpm generate:importmap
pnpm generate:types
```

### 6. Seed placeholder content

```bash
pnpm seed
```

(Idempotent — re-run anytime to reset to defaults. Skips if `DATABASE_URI` not set.)

### 7. Start dev server

```bash
pnpm dev
```

- Frontend: http://localhost:3000
- Admin: http://localhost:3000/admin (first visit asks you to create an account)

## Project structure

```
src/
├── app/
│   ├── (frontend)/           # Public marketing site
│   │   ├── layout.tsx        # Fonts, metadata
│   │   ├── page.tsx          # Home — fetches all CMS data
│   │   └── _components/      # Section components (Phase 3)
│   ├── (payload)/            # Admin panel + API
│   │   ├── admin/            # Auto-generated by Payload
│   │   ├── api/              # REST + GraphQL routes
│   │   └── layout.tsx        # Payload admin layout
│   └── globals.css           # Tailwind + design tokens + animations
├── collections/              # Payload collections (multi-entry content)
│   ├── Services.ts           # 4 service pillars
│   ├── Cases.ts              # Work case studies
│   ├── Products.ts           # Studio products (Laporta, Viralytics)
│   ├── ProcessPhases.ts      # 4 phases
│   ├── Tenets.ts             # 3 studio principles
│   ├── FAQs.ts
│   ├── Clients.ts            # Trusted-by
│   ├── Users.ts              # Admin auth
│   └── Media.ts              # File uploads
├── globals/                  # Payload globals (single-instance content)
│   ├── Hero.ts               # Hero section
│   ├── Studio.ts             # About copy + stats
│   ├── Contact.ts            # Form labels + heading
│   ├── TopBar.ts             # Announcement bar
│   └── SiteSettings.ts       # Nav, footer, OG image
├── lib/
│   ├── icons.tsx             # 11 inline SVG primitives
│   └── payload.ts            # Local API helper
└── payload.config.ts         # Payload root config
```

## Design system

Colors and typography tokens are defined in `src/app/globals.css` via Tailwind v4 `@theme` directive.

| Token | Value | Use |
|---|---|---|
| `--color-electric` | `#2C70FE` | Primary accent (sparingly) |
| `--color-ink` | `#08090A` | Foreground / dark surface |
| `--color-paper` | `#F4F7F5` | Default background |
| `--color-mist` | `#A7A2A9` | Muted text / borders |
| `--font-sans` | Satoshi | UI / display |
| `--font-mono` | JetBrains Mono | Code, status, mono labels |

## Deployment

```bash
# Vercel (auto-deploys on push)
git push origin main
```

Production URL set via `NEXT_PUBLIC_SERVER_URL` env var.

## Reference

The original static site (HTML + babel-CDN JSX) is preserved in `_legacy/` for reference during Phase 3 migration. After migration is verified, this folder can be deleted.
